import DraggableScreen from "../DraggableScreen";

const CertificateScreen = ({ onClose }) => {
  return (
    <DraggableScreen title="Certificates" defaultWidth={500} defaultHeight={350} onClose={onClose}>
      
    </DraggableScreen>
  );
};

export default CertificateScreen;
