import DraggableScreen from "../DraggableScreen";

const ContactScreen = ({ onClose }) => {
  return (
    <DraggableScreen title="Contact" defaultWidth={400} defaultHeight={250} onClose={onClose}>
      
    </DraggableScreen>
  );
};

export default ContactScreen;
