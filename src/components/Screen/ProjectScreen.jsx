import DraggableScreen from "../DraggableScreen";


const ProjectScreen = ({ onClose }) => {
  return (
    <DraggableScreen title="Projects" defaultWidth={600} defaultHeight={400} onClose={onClose}>
      
    </DraggableScreen>
  );
};

export default ProjectScreen;
